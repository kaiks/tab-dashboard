<%= form_with(model: list, local: true) do |form| %>
  <% if list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(list.errors.count, "error") %> prohibited this list from being saved:</h2>

      <ul>
      <% list.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :list_title %>
  </div>

  <div class="field">
    <%= form.label :position %>
    <%= form.number_field :position, id: :list_position %>
  </div>

  <div class="field">
    <%= form.label :preview_limit %>
    <%= form.number_field :preview_limit, id: :list_preview_limit %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
