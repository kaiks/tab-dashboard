<p id="notice"><%= notice %></p>

<h1>To Do Items</h1>

<table>
  <thead>
    <tr>
      <th>User</th>
      <th>Content</th>
      <th>Done</th>
      <th>Removed</th>
      <th>Done at</th>
      <th>Description</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @to_do_items.each do |to_do_item| %>
      <tr>
        <td><%= to_do_item.user_id %></td>
        <td><%= to_do_item.content %></td>
        <td><%= to_do_item.done %></td>
        <td><%= to_do_item.removed %></td>
        <td><%= to_do_item.done_at %></td>
        <td><%= to_do_item.description %></td>
        <td><%= link_to 'Show', to_do_item %></td>
        <td><%= link_to 'Edit', edit_to_do_item_path(to_do_item) %></td>
        <td><%= link_to 'Destroy', to_do_item, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New To Do Item', new_to_do_item_path %>
